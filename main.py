"""
Основной файл запуска GBR Security System
"""

from app.config import config
from app.database import init_db


def main():
    """Главная функция запуска системы"""
    try:
        # Загрузка конфигурации (происходит автоматически при импорте)
        print("✅ Конфигурация загружена")
        print(f"   - Dispatcher ID: {config.dispatcher_id}")
        print(f"   - Database URL: {config.database_url}")
        
        # Инициализация базы данных
        init_db()
        
        # Система успешно инициализирована
        print("🎉 GBR Security System успешно инициализирована!")
        print("Система готова к работе с Telegram-ботами")
        
    except Exception as e:
        print(f"❌ Ошибка инициализации системы: {e}")
        return 1
    
    return 0


if __name__ == "__main__":
    exit(main())
